{{ansible_managed|comment(decoration='#')}}

check process ifstated matching "/usr/sbin/ifstated"
  start program = "/usr/sbin/rcctl start {{service_name_ifstated}}"
  stop program = "/usr/sbin/rcctl stop {{service_name_ifstated}}"
  if does not exist for 3 cycles then alert
